import React from "react";
import { View, Text, Button, StyleSheet } from "react-native";

const Boilerplate = () => {
  return <Text>Boilerplate</Text>;
};

const styles = StyleSheet.create({});

export default Boilerplate;
